<!--
  Small house: a square body + triangular roof, on a 24x24 design grid.
  Uses currentColor so the caller can recolor per owner via a wrapping
  `style="color: ..."`, unlike the fixed-palette terrain/resource icons.
-->
<g fill="currentColor" stroke="#000000" stroke-width="0.8">
  <rect x="6" y="13" width="12" height="8" />
  <polygon points="4,13 12,5 20,13" />
</g>
